// Define a function that takes a name as a string
function greet(name: string): string {
    return `Hello, ${name}!`;
  }
  
  // Call the function and store the result
  let message = greet("Alice");
  
  // Print the message to the console
  console.log(message);
  


